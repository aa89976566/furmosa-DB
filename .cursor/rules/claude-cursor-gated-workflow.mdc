---
description: 每個程式任務強制執行 Claude 三題審查、Cursor 範圍鎖定與獨立驗證
alwaysApply: true
---

# Claude → Cursor 強制工作閘門

所有功能、修正、重構、UI、設定與部署任務都必須遵守本規則。

## 1. 任務凍結

先閱讀需求、相關檔案與專案規則，建立任務卡：

- 單一目標
- 現況證據
- 允許修改的檔案或目錄
- 禁止修改的功能與檔案
- 驗收條件
- 已知風險

任務範圍不明確時停止，先向使用者確認。

## 2. Claude 三題審查

把同一張任務卡直接送給 Claude，並依序個別提出三題。每題必須等待 Claude 回覆後才能提出下一題。

1. 這個任務最小且安全的修改範圍是什麼？哪些檔案或功能不得改動？
2. 哪些回歸、邊界案例、安全或資料風險最容易被忽略？
3. 應使用哪些驗收條件、測試與回復方式，才能證明修改可安全交付？

保存三次實際回覆，整理：

- 共識
- 分歧
- 採用方案
- 不採用建議與理由

無法直接連接 Claude 時停止並回報。不得產生、假造或代寫 Claude 回覆。

## 3. 鎖定 Cursor Prompt

Claude 審查完成後，產生版本化 prompt，例如 `CURSOR-PROMPT-v1`。Prompt 必須包含：

1. 目標
2. 現況證據
3. 允許修改範圍
4. 禁止修改範圍
5. 逐步執行流程
6. 驗收條件
7. 測試指令
8. 停止條件
9. 回報格式

Cursor 不得改寫、重新解釋或擴張 prompt。任何需求變更都要建立新版 prompt，並重新執行 Claude 三題審查。

## 4. 執行前基準

送出 prompt 前記錄：

- `git status --short --branch`
- `git diff --stat`
- 當前 commit
- 允許修改檔案清單

工作樹若已有不相關修改，不得覆蓋、清除或混入本任務。

## 5. Cursor 執行監督

Cursor 每完成一個步驟就回報修改檔案與原因。監督者持續核對：

- 是否只修改允許範圍
- 是否保留既有功能
- 是否沒有降低測試或安全檢查
- 是否沒有新增未授權套件、資料庫或正式環境變更

發現越界立即停止，不得測試、提交、合併或部署。先列出越界差異並要求 Cursor 修正。

## 6. 獨立驗證

Cursor 完成後，監督者必須自行檢查：

- `git status --short`
- `git diff --stat`
- 完整 `git diff`
- 相關 lint、typecheck 與測試
- 手機及桌機關鍵流程

不得只依賴 Cursor 的完成聲明。測試失敗或驗收未完成時，不得宣稱完成。

## 7. 提交與部署

- 只提交本任務允許範圍內的變更。
- 部署前重新同步最新 `main` 並確認沒有覆蓋其他工作。
- 未經使用者明確同意，不得合併或部署正式環境。
